# AC V15.0.29 — PinDataGuard

- Các thao tác Cloud Sync nhạy cảm bắt buộc nhập PIN.
- Các thao tác Dữ liệu/Backup nhạy cảm bắt buộc nhập PIN.
- PIN không lưu dạng thô, chỉ dùng hash nội bộ để kiểm tra.
- Khối JSON nhanh mặc định collapse, có nút expand/collapse.
